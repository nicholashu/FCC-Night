'use strict';

(function () {
	angular
		.module('BarController', ['clementineBarApp'])
		.controller('BarCtrl',
			['$scope',
			'$resource',
			function ($scope, $resource) {
			//functions go in here
		}]);
})();
